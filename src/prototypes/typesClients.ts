export enum Gender {
  m = "Male",
  f = "Female",
}

export enum CivilStatus {
  married = "Married",
  single = "Single",
  divorced = "Divorced"
}

export enum TypeDocuments {
  dni = 'Identification card',
  pa = 'Passport',
}